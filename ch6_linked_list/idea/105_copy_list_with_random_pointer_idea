1. Create all empty new nodes and store them in a hashmap (dict). 
2. newNodeDict[pointer] = RandomListNode(pointer.label)
3. Run a for loop to set next and random of all new nodes.
4. if node.next -> newNodeDict[node].next = newNodeDict[node.next]
5. if node.random -> newNodeDict[node].random = newNodeDict[node.random]
6. return newNodeDict[head]