1. It's similar to balance quick sort.
2. Be aware of the problem's definition: < k are moved to the left, >= k are moved to the right.
3. In the end, if pointer left == pointer right, when nums[left] < k -> return left+1.